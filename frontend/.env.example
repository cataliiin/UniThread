# ==============================================================================
# UniThread - Frontend SvelteKit Environment Reference Template (.env.example)
# ==============================================================================

# API Endpoint URL (where SvelteKit client fetches resources from FastAPI)
PUBLIC_API_URL=http://localhost:8000

# Storage Endpoint URL (where SvelteKit client fetches files from MinIO)
VITE_STORAGE_URL=http://localhost:9000
